# European Era — Website

Modern institutional + lead generation website for European Era (Erasmus, internships, mobility support).

## Tech
- Next.js (App Router) + React + TypeScript
- Tailwind CSS

## Getting started
Install dependencies and run dev server:

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

